{
  "name": "nestjs-base-template",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start:dev --parallel",
    "start:main": "turbo run start:dev --filter @main",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.7",
    "prettier": "^3.5.3",
    "turbo": "^2.5.2",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=18"
  }
}
